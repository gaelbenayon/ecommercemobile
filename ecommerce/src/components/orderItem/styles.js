import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
    ordersContainer: {
        marginBottom: 15
    },
    orderInfo: {
        marginBottom: 10
    },
    orderContainer: {
        marginBottom: 10,
        flexDirection: 'row'
    },
    orderImage: {
        width: 50,
        height: 50,
        marginRight: 5
    }
})